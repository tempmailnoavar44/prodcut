import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images:
    {
      domains: [
        "noav.ir",
        "noavarpub.com",
        "https://noavarpub.com/wp-content/uploads/",
        "static.cdn.asset.aparat.com",
      ],
    },
};

export default nextConfig;
